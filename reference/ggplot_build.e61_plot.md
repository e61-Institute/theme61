# Method for theme61 plots to add default scales at build time

Method for theme61 plots to add default scales at build time

## Usage

``` r
# S3 method for class 'e61_plot'
ggplot_build(plot, ...)
```
