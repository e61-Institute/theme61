# Instructions for Claude

- Comments: keep them terse. One short line max per comment. Only comment on
  non-obvious WHY (a hidden constraint, a subtle invariant, a workaround for
  a specific bug) - never WHAT the code does, never multi-line explanations,
  never a paragraph justifying a fix.
