# Instructions for Claude

- Comments: keep them concise and succinct - a sentence or two, not a
  paragraph. Only comment on non-obvious WHY (a hidden constraint, a
  subtle invariant, a workaround for a specific bug) - never WHAT the
  code does.
