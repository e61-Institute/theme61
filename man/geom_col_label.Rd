% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_col_label.R
\name{geom_col_label}
\alias{geom_col_label}
\title{Add automatic percentage labels to column charts}
\usage{
geom_col_label(
  mapping = NULL,
  data = NULL,
  ...,
  accuracy = 1,
  align = "top",
  reverse = FALSE,
  position = "stack",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.
Requires \code{x} and \code{y}; add \code{fill} (as you would for \code{\link[ggplot2:geom_col]{ggplot2::geom_col()}})
to create stacked segments.}

\item{data}{The data to be displayed in this layer.}

\item{...}{Other arguments passed on to \code{\link[ggplot2:geom_text]{ggplot2::geom_text()}}, e.g.
\code{colour} or \code{size}.}

\item{accuracy}{Numeric. Passed to \code{\link[scales:label_percent]{scales::label_percent()}} to control
rounding, e.g. \code{accuracy = 0.1} shows one decimal place. Defaults to \code{1}
(whole percentages).}

\item{align}{Where to position the label. One of \code{"top"}, \code{"middle"},
\code{"bottom"}, or a number from 0 (bottom of the column/segment) to 1 (top
of the column/segment). Defaults to \code{"top"}. For single (non-stacked)
columns, \code{"top"} floats the label just above the column, and \code{"bottom"}
sits it just inside the column above its base - both leave a small gap
rather than sitting flush against the column. For stacked columns,
\code{"top"}/\code{"bottom"} sit just inside \strong{each segment's own} top/bottom
edge (not just the outer edge of the stack as a whole) with the same
gap. Any other value centres the label inside the column/segment at
that fraction of its height.}

\item{reverse}{Logical. Reverse the stacking order used to position labels
within stacked columns. Set this to match \code{position_stack(reverse = TRUE)} if you used that for your \code{geom_col()}. Defaults to FALSE.}

\item{position}{One of \code{"stack"} (the default), \code{"dodge"}, or \code{"dodge2"}.
Set this to match the \code{position} you used on the corresponding
\code{\link[ggplot2:geom_col]{ggplot2::geom_col()}} call. \code{"dodge"}/\code{"dodge2"} split each bar's width
the same way \code{\link[ggplot2:position_dodge]{ggplot2::position_dodge()}}/\code{\link[ggplot2:position_dodge2]{ggplot2::position_dodge2()}}
do (based on the \code{fill}/\code{group} aesthetic), and each label shows that
bar's own share of the panel-wide total rather than a share of its
(non-existent) stack.}

\item{na.rm}{If FALSE, the default, missing values are removed with a
warning. If TRUE, missing values are silently removed.}

\item{show.legend}{logical. Should this layer be included in the legends?}

\item{inherit.aes}{If FALSE, overrides the default aesthetics.}
}
\value{
Object to add to a ggplot (via \code{+}).
}
\description{
Adds a text label above (or inside) each column showing its
share of a total, as a percentage. Percentages are calculated
automatically from the data, so there is no need to pre-compute a
percentage column:
\itemize{
\item For \strong{stacked} columns (i.e. more than one value sharing
the same \code{x}, typically via a \code{fill} aesthetic), each label shows that
segment's share of its column's stack.
\item For \strong{single} (non-stacked) columns, each label shows that
column's share of the total across all columns in the panel.
}
}
\details{
Works the same way with \code{\link[ggplot2:coord_flip]{ggplot2::coord_flip()}}: the labels are
computed in data space (before the flip is applied), so \code{align = "top"}
still means "furthest from zero", which renders past the end of a
horizontal column once flipped.

Every edge-aligned label leaves a gap between itself and the column -
scaled to the data's own range, so it looks proportionate whether the
y-axis runs from 0 to 10 or 0 to 100,000 - and only one of them needs
any extra space \emph{beyond} the columns to do that: a single (non-stacked)
column's \code{align = "top"} label genuinely floats outside the column, so
a small amount of headroom is reserved automatically beyond the tallest
column for it (this matters because theme61's default
\code{scale_y_continuous_e61()} has no expansion at the data max/min).
\code{"bottom"} on a single column, and both ends of a stacked column, sit
just inside the column with their own gap nudged inward from the edge
instead, so they need no reserved space beyond the column at all.

An explicit \code{scale_y_continuous_e61(limits = ...)} always takes
precedence: the reserved top headroom and the single-column gap are
capped at the supplied limit rather than nudging past it, since
\code{scale_y_continuous_e61()} errors if data falls outside a limit you've
set. If your limit sits exactly at (or inside) the data's own range,
the label may end up flush against the edge again - widen the limit if
you want the gap back.
}
\examples{
library(ggplot2)

# Single columns: label shows each column's share of the total
df <- data.frame(grp = c("A", "B", "C"), value = c(10, 30, 60))

ggplot(df, aes(grp, value)) +
  geom_col() +
  geom_col_label()

# Stacked columns: label shows each segment's share of its column
df2 <- data.frame(
  x = rep(c("2023", "2024"), each = 2),
  grp = rep(c("Group 1", "Group 2"), 2),
  value = c(30, 70, 45, 55)
)

ggplot(df2, aes(x, value, fill = grp)) +
  geom_col() +
  geom_col_label(align = "middle", colour = "white")

# Works the same way flipped
ggplot(df, aes(grp, value)) +
  geom_col() +
  geom_col_label() +
  coord_flip()

}
