% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/e61_map.R
\name{add_map_e61}
\alias{add_map_e61}
\alias{setup_stadia_maps}
\title{Add an underlying base map to ggplot maps}
\usage{
add_map_e61(
  bbox = c(top = -33.757742, right = 151.492882, bottom = -34.024779, left = 150.839539),
  adjust = 0,
  maptype = "stamen_toner_lite"
)

setup_stadia_maps()
}
\arguments{
\item{bbox}{A named vector of numeric coordinates defining the extent of your
map, e.g \code{c(top = -33, right = 151, bottom = -34, left = 150)}. Defaults to
bounds for Greater Sydney.}

\item{adjust}{Numeric. Adjust default zoom value.}

\item{maptype}{Character. Select a valid map type from
\code{\link[ggmap:get_stadiamap]{ggmap::get_stadiamap()}}'s \code{maptype} argument. The default is set to
\code{stamen_toner_lite}.}
}
\value{
A ggmap object
}
\description{
Wrapper around \code{\link[ggmap:get_stadiamap]{ggmap::get_stadiamap()}} that makes it easy to insert a base
map underneath a ggplot-based map. The default map type (Stamen's Toner Lite
map) is designed to be unintrusive so the focus remains on the data being
visualised.

Using Stadia Maps (formerly Stamen Maps) tiles requires setting up a free API
key and registering it in your R session, this helper function helps you set
up your session accordingly.
}
\details{
To get an API key, you must sign up at
\link{https://client.stadiamaps.com/signup/}.
}
\examples{

library(sf)
sa3 <- dplyr::filter(strayr::read_absmap("sa32021"), gcc_code_2021 == "1GSYD") # let's just look at Sydney

 ggplot(sa3) +
 add_map_e61(bbox = c(
   left = min(sa3$cent_long),
   bottom = min(sa3$cent_lat),
   right = max(sa3$cent_long),
   top = max(sa3$cent_lat))
   ) +
 geom_point(aes(x = cent_long, y = cent_lat)) + #plot points
 theme_e61_spatial()

}
\seealso{
Other map functions: 
\code{\link{crop_aus_coord}()},
\code{\link{theme_e61_spatial}()}

Other map functions: 
\code{\link{crop_aus_coord}()},
\code{\link{theme_e61_spatial}()}
}
\concept{map functions}
