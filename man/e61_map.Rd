% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/e61_map.R
\name{e61_map}
\alias{e61_map}
\title{Wrapper function for ggmap to produce a map layer in e61 format}
\usage{
e61_map(
  bbox = c(top = -33.757742, right = 151.492882, bottom = -34.024779, left = 150.839539),
  adjust = 0,
  maptype = "toner-lite"
)
}
\arguments{
\item{bbox}{A named vector of coordinates decribing the extent of your map,
e.g \code{c(top = -33, right = 151, bottom = -34, left = 150)}}

\item{adjust}{Adjust default zoom value}

\item{maptype}{Set as the default map type for the e61 style, you can change to another
maptype if needed}
}
\description{
Simple wrapper around \code{\link[ggmap:get_stamenmap]{ggmap::get_stamenmap()}} that makes it easier to
overlay a map layer for geographical data in ggplot
}
\details{
The primary purpose of this function is to add a map layer with major place
names and roads from Stamen's map server to plot under geographical data.
}
\examples{

library(sf)
sa3 <- dplyr::filter(strayr::read_absmap("sa32021"), gcc_code_2021 == "1GSYD") # let's just look at Sydney

 ggplot(sa3) +
 e61_map(bbox = c(
   left = min(sa3$cent_long),
   bottom = min(sa3$cent_lat),
   right = max(sa3$cent_long),
   top = max(sa3$cent_lat))
   ) +
 geom_point(aes(x = cent_long, y = cent_lat)) #plot points


}
